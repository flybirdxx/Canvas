## 2026-05-09 - Add aria labels to buttons
**Learning:** Found several icon-only buttons in the UI (History panel, Alignment Toolbar, Settings) lacking ARIA labels for screen readers.
**Action:** Always ensure icon-only buttons receive an `aria-label` alongside the visual `title` tag for a11y support.
